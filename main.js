const todaysDate = () => {
    console.log(new Date());
    };
    todaysDate();
    
    
    const isSqaured = (x) => {
        let root = Math.sqrt(x);
        return root % 1 == 0;
    };
    console.log(isSqaured(10));
    console.log(isSqaured(9));
    
    